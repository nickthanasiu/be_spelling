import { deriveRankingFromScore } from "../state/ranking";

export function useRanking() {

    const score = 1;
    const rankings = {

    };

    //deriveRankingFromScore(score, rankings);

    return '';
}