export const ensureLowerCase = (str: string): string => {
    return str.toLowerCase();
};

export const checkEquality = (str1: string, str2: string) => {
    return str1.toLowerCase() === str2.toLowerCase();
}